# 前端跨域问题排查与解决方案

## 1. 问题背景

本项目曾出现以下现象：

- 在浏览器页面中调用登录接口失败。
- 使用 Postman、Apifox、curl 等 API 工具调用同一个地址成功。
- 页面与 API 工具提交的接口地址、用户名、密码看起来完全一致。

原登录接口地址为：

```text
http://localhost:6000/sys/user/login
```

前端开发服务器通常运行在：

```text
http://localhost:8080
```

由于协议、主机、端口三者中端口不同，这两个地址属于不同源。浏览器会执行同源策略和 CORS 检查，而 API 工具通常不会执行浏览器的同源策略，因此会出现“API 工具成功、页面失败”的情况。

## 2. 为什么请求内容相同，结果仍然不同

浏览器请求与 API 工具请求的主要区别不在业务参数，而在请求执行环境。

### 2.1 浏览器受同源策略限制

浏览器会比较当前页面来源与接口来源：

```text
页面来源：http://localhost:8080
接口来源：http://localhost:6000
```

端口不同即为跨域。浏览器只有在后端返回正确的 CORS 响应头后，才允许前端 JavaScript 读取响应。

### 2.2 JSON POST 会触发预检请求

登录请求使用：

```http
Content-Type: application/json
```

浏览器通常会先发送 `OPTIONS` 预检请求，确认后端是否允许：

- 当前页面的 `Origin`
- `POST` 请求方法
- `Content-Type` 请求头
- Cookie 或其他凭证

预检不通过时，浏览器不会正常执行或暴露后续 `POST` 响应。API 工具一般会直接发送 `POST`，所以不受该过程影响。

### 2.3 浏览器还可能受到其他网络安全策略影响

即使后端已经配置 CORS，浏览器直连后端仍可能受到以下因素影响：

- HTTPS 页面请求 HTTP 接口产生混合内容拦截。
- 公网页面访问本机或局域网地址受到私有网络访问策略限制。
- 页面实际端口发生变化，导致允许来源与当前来源不一致。
- 浏览器插件、代理软件、VPN 或安全软件修改或拦截请求。
- 使用 `localhost`、`127.0.0.1`、局域网 IP 时，实际来源并不相同。

## 3. 本次排查结论

排查过程中已核对前后端请求契约。

### 3.1 请求体一致

前端提交：

```json
{
  "username": "用户名",
  "password": "密码"
}
```

后端 `LoginUserForm` 同样接收：

```text
username
password
```

因此不存在前端字段名与后端字段名不一致的问题。

### 3.2 响应结构一致

后端成功响应结构为：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "accessToken": "登录令牌"
  }
}
```

前端成功条件为：

```js
response.ok &&
result.code === 200 &&
result.data &&
result.data.accessToken
```

该判断与后端当前返回结构一致。

### 3.3 后端预检响应正常

使用带 `Origin`、`Access-Control-Request-Method` 和 `Access-Control-Request-Headers` 的 `OPTIONS` 请求检查后，后端能够返回：

```http
Access-Control-Allow-Origin: 当前页面来源
Access-Control-Allow-Methods: GET,POST,PUT,DELETE
Access-Control-Allow-Headers: content-type
Access-Control-Allow-Credentials: true
```

这说明后端当前并非完全没有 CORS 配置。问题更可能来自浏览器直连不同端口时的环境差异，因此项目最终采用同源代理方案，避免让浏览器直接访问 `localhost:6000`。

## 4. 最终解决方案：使用开发服务器同源代理

浏览器只访问前端当前来源下的 `/api`，由 Vue CLI 开发服务器将请求转发到后端。

请求链路变为：

```text
浏览器
  -> http://localhost:8080/api/sys/user/login
  -> Vue CLI 开发代理
  -> http://localhost:6000/sys/user/login
```

从浏览器角度看，请求仍然发送给 `localhost:8080`，属于同源请求，因此不再由浏览器对 `localhost:6000` 执行跨域检查。

### 4.1 Vue CLI 代理配置

项目的 `vue.config.js` 配置如下：

```js
const { defineConfig } = require('@vue/cli-service')

const apiTarget = process.env.VUE_APP_DEV_API_TARGET || 'http://localhost:6000'

module.exports = defineConfig({
  transpileDependencies: true,
  devServer: {
    proxy: {
      '/api': {
        target: apiTarget,
        changeOrigin: true,
        pathRewrite: {
          '^/api': ''
        }
      }
    }
  }
})
```

各配置项作用：

| 配置项 | 作用 |
| --- | --- |
| `/api` | 匹配所有以 `/api` 开头的开发请求 |
| `target` | 后端服务地址，默认是 `http://localhost:6000` |
| `changeOrigin` | 将代理请求的 Host 调整为目标服务 |
| `pathRewrite` | 转发前删除 `/api` 前缀 |

例如：

```text
/api/sys/user/login
```

转发后为：

```text
http://localhost:6000/sys/user/login
```

### 4.2 前端登录地址

`src/constants/auth.js` 使用相对地址：

```js
const apiBaseUrl = (process.env.VUE_APP_API_BASE_URL || '/api').replace(/\/+$/, '')

export const LOGIN_URL = `${apiBaseUrl}/sys/user/login`
export const ACCESS_TOKEN_KEY = 'accessToken'
```

开发环境默认请求：

```text
/api/sys/user/login
```

不要在业务代码中继续写死：

```text
http://localhost:6000/sys/user/login
```

否则浏览器会绕过开发代理，再次变成跨域直连。

### 4.3 登录请求实现

当前登录请求仍然使用 JSON，不需要修改业务参数：

```js
response = await fetch(LOGIN_URL, {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(credentials)
})
```

代理只改变网络链路，不改变接口请求体和后端响应结构。

## 5. 环境变量

项目支持以下环境变量：

| 环境变量 | 默认值 | 用途 |
| --- | --- | --- |
| `VUE_APP_DEV_API_TARGET` | `http://localhost:6000` | 修改 Vue CLI 开发代理的后端目标 |
| `VUE_APP_API_BASE_URL` | `/api` | 修改浏览器请求使用的接口前缀或完整基础地址 |

例如，后端改为 `127.0.0.1:6001` 时，可以在开发环境变量中配置：

```dotenv
VUE_APP_DEV_API_TARGET=http://127.0.0.1:6001
```

修改 `vue.config.js` 或开发代理相关环境变量后，必须重新启动前端开发服务器。开发服务器不会在运行期间自动重新加载代理配置。

## 6. 生产环境配置

Vue CLI 的 `devServer.proxy` 只在开发服务器中生效，打包后的静态文件不会携带一个 Node.js 代理服务器。

生产环境推荐由 Nginx、网关或部署服务器代理 `/api`。

Nginx 示例：

```nginx
location /api/ {
    proxy_pass http://127.0.0.1:6000/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

此配置会将：

```text
https://前端域名/api/sys/user/login
```

转发为：

```text
http://127.0.0.1:6000/sys/user/login
```

生产环境也可以通过 `VUE_APP_API_BASE_URL` 指向完整后端地址，但这会重新产生跨域、HTTPS 和私有网络访问问题。优先推荐前后端共用域名并通过 `/api` 反向代理。

## 7. 验证方式

重新启动前端开发服务器后，在浏览器开发者工具的 Network 面板中检查登录请求。

### 7.1 正确表现

浏览器中显示的请求地址应类似：

```text
http://localhost:8080/api/sys/user/login
```

请求体应为：

```json
{
  "username": "实际用户名",
  "password": "实际密码"
}
```

后端日志中实际收到的路径应为：

```text
/sys/user/login
```

成功响应应满足：

```text
HTTP 状态为 2xx
code 为 200
data.accessToken 不为空
```

### 7.2 常见状态判断

| 表现 | 常见原因 |
| --- | --- |
| 浏览器仍请求 `localhost:6000` | 前端仍使用写死的完整地址，或开发服务器未重启 |
| `/api` 返回 404 | 代理路径重写不正确，或后端接口路径不一致 |
| `/api` 返回 502/504 | 后端未启动、端口错误或代理目标无法连接 |
| 返回 HTML，页面提示无效数据 | 请求落到前端页面或代理未命中 |
| HTTP 成功但页面提示登录失败 | 检查 `code`、`message` 和 `data.accessToken` |
| Network 中显示 CORS 或 Mixed Content | 请求仍在绕过同源代理直连后端 |
| 修改代理后行为没有变化 | 需要重新启动 Vue CLI 开发服务器 |

## 8. 调试日志说明

开发环境登录时，项目会在浏览器 Console 中输出：

- 完整请求地址
- 页面来源
- 请求数据
- 可在 Windows PowerShell 中执行的 `curl.exe` 命令
- HTTP 响应状态和响应数据

响应日志会隐藏 `accessToken`，但调试 cURL 和请求数据中包含登录密码，只能用于本地排查，不能复制到日志平台、聊天记录或生产环境。

生产构建中不会输出这些调试日志。

## 9. 直接配置后端 CORS 的备选方案

如果不使用前端代理，后端必须完整允许前端来源、方法和请求头。Spring MVC 示例：

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {

    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**")
                .allowCredentials(true)
                .allowedOriginPatterns("*")
                .allowedMethods("GET", "POST", "PUT", "DELETE")
                .allowedHeaders("*")
                .exposedHeaders("*");
    }
}
```

实际生产环境不建议长期使用过于宽泛的 `allowedOriginPatterns("*")`，应限制为可信前端域名。

即使 CORS 配置正确，HTTPS 页面访问 HTTP 接口、网页访问用户本机 `localhost` 等场景仍可能失败。因此，本项目优先使用同源反向代理，而不是依赖浏览器跨域直连。

## 10. 最终结论

“API 工具请求成功”只能证明后端接口地址和业务参数基本可用，不能证明浏览器一定可以调用该接口。

本项目采用以下统一原则：

1. 前端业务代码只请求 `/api` 相对地址。
2. 开发环境由 Vue CLI 代理到 `http://localhost:6000`。
3. 生产环境由 Nginx 或网关代理 `/api`。
4. 不在业务代码中写死开发机后端地址。
5. 修改代理配置后重新启动前端开发服务器。
6. 通过 Network 面板区分网络失败、响应解析失败和业务校验失败。

这样可以避免大多数 CORS、混合内容、端口变化和本地网络访问策略导致的“页面失败但 API 工具成功”问题。
