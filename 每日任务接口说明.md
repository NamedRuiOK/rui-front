# 每日任务接口说明

## 1. 分页查询每日任务

- 请求方式：`POST`
- 请求地址：`/api/user/dayTask/list`
- 页面调用：`src/api/day-task.js`
- 首页调用参数：

```json
{
  "pageSize": 10,
  "pageNum": 2
}
```

请求头需要携带登录后的 Token：

```text
Authorization: {accessToken}
Content-Type: application/json
```

成功响应：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [],
    "total": 1,
    "pageNum": 2,
    "pageSize": 10,
    "pages": 1
  }
}
```

前端使用 `data.records` 作为任务数组，分页元数据保留在 `data` 中。任务记录至少需要提供以下字段：

| 字段 | 类型 | 用途 |
| --- | --- | --- |
| `id` | number/string | 任务唯一标识，完成任务时使用 |
| `name` | string | 任务名称 |
| `description` | string | 任务描述，可选 |
| `priority` | number | 优先级，前端按 1-5 映射显示 |
| `status` | number | 任务状态，`1` 为进行中，`2` 为已完成 |
| `startTime` | string | 开始时间，可选 |
| `endTime` | string | 截止时间，可选 |
| `completedTime` | string | 完成时间，可选 |
| `labelIds` | array | 标签 ID，可选 |

## 2. 完成每日任务

- 请求方式：`POST`
- 请求地址：`/api/user/dayTask/complete`
- 请求参数：

```json
{
  "id": 123
}
```

页面勾选任务后调用该接口。接口成功需要返回 `code === 200`，页面随后将当前任务状态更新为已完成。

## 3. 前端处理约定

- `code` 不等于 `200` 时，页面显示接口返回的 `message`。
- 网络异常或响应不是合法 JSON 时，页面显示可重试的错误状态。
- `data.records` 不是数组时，页面按数据格式错误处理。
- `records` 为空数组时，页面显示“今天还没有安排任务”。
- 当前首页按照需求示例请求第 `2` 页；后续接入分页控件时，应将 `pageNum` 改为当前页码，而不是固定值。
